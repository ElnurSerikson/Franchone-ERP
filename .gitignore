node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
npm-debug.log*

# Внутренние материалы компании — остаются только локально, не пушатся
docs/

# Локальные настройки Claude
.claude/settings.local.json

# TypeScript build cache
*.tsbuildinfo
